"""
constants/__init__.py

This file stores all constant values used across the project.

Keeping constants in one place avoids hardcoding
the same values in multiple files.
"""

from pathlib import Path

# -----------------------------------------------------
# Configuration File Path
# -----------------------------------------------------

CONFIG_FILE_PATH = Path("config/config.yaml")